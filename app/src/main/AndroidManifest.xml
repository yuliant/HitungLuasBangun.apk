<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.hitungluasbangun">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/lingkaran"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/lingkaran"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".persegi" />
        <activity android:name=".segitiga" />
        <activity android:name=".lingkaran"></activity>
    </application>

</manifest>